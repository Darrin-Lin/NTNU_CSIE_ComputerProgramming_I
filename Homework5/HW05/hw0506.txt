因為在輸入時會是 32 bit 的整數，array 是 16 bit，在輸入時由後往前，數字是小的正整數，所以因為 Little Endian，較小位數的數字存在較前面的記憶體，然後後面 8 bit 就會是 0，所以會剛好蓋掉後面一個數字，循環後五次後只剩最前面的。
而設定 10 是因為如果是開 5 就會在寫入 index = 4 的時候寫入到後面 16 bit 不該寫入的地方，所以至少要開 6。